# Runtime dependencies — AI Climate Resilience Sandbox (mirrors repo-root requirements.txt)
# Install:  pip install -r requirements.txt
# Run:      streamlit run frontend/app.py   (from the repo root)
streamlit>=1.32
plotly>=5.18
pandas>=2.0
numpy>=1.26
fpdf2>=2.7
scikit-learn>=1.4
joblib>=1.3
